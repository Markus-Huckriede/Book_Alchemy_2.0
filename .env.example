SECRET_KEY=replace-with-a-long-random-secret

# Optional for local Postgres or production on Vercel.
# Without this, local development uses library.sqlite.
# On Vercel, omitting DATABASE_URL uses ephemeral SQLite in /tmp for test deploys only.
DATABASE_URL=
